First Release
=============
* Dynamically create lists of FROM tables
* Make sure you can do an inner join with:
  foo.id = bar.foo_id
* Alias field names
* VALUES can take a subselect
* POD

Future Versions
===============
* Custom SQL function calls: 
    date_entered > to_date(?, 'MM/DD/YYYY')
  With bind var "11/26/2008"
